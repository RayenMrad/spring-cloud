server:
  port: 8082
spring:
  application:
    name: chanson
  datasource:
    url: 'jdbc:h2:mem:chansonsdb'
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
management:
  endpoints:
    web:
      exposure:
        include: '*'